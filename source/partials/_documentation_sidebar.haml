- data.sidebar.each do |(category, pages)|
  %h1= category.name
  %ul
    - category.pages.each do |page_name, url|
      - path = 'documentation/' + url + 'index.html'
      %li
        = link_to page_name, '/' + path
